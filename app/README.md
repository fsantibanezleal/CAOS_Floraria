# Runtime API: dormant

This solution does not require it at the moment. FLORARIA serves a static application and versioned public assets. There is no FastAPI service, database, authentication or request-time processing engine.

Source processing lives in data-pipeline; the browser lives in frontend. A future server capability needs a concrete requirement and its own security/state/deployment contract. The example API was removed so it cannot be mistaken for supported functionality.
